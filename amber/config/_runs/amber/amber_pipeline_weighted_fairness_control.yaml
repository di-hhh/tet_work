# @package _global_

defaults:
  - amber_pipeline_weighted
  - _self_

exp_name: amber_pipeline_weighted_fairness_control
_version: 1
idx: ambpw1ft

algorithm:
  # Load M1 weights only; Trainer creates a fresh optimizer and scheduler.
  init_from_weighted_baseline_checkpoint: auto

experiment_protocol:
  method_id: M1
  analysis_id: M1-FT
  method_role: auxiliary
  protocol_id: formal-mold7000-v3
  formal_run: true
  run_test_after_fit: false
  evaluation_prediction_variants: [final]
  budget_close_relative_tolerance: 0.18
  budget_valid_min_ratio: 0.8
  budget_valid_max_ratio: 1.5714285714285714
  require_seed_matched_m1_checkpoint: true
  initialization_mode: weights_only_fresh_optimizer
  evaluation_checkpoint: last.ckpt
  training_budget_epochs: 101
  require_clean_repositories: true
  m1_checkpoint_search_root: output/hydra/training
