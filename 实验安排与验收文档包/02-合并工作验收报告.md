# 合并工作验收报告

## 文档信息

- **生成时间（北京时间）**：2026年08月14日 03:34:09
- **最近修改时间（北京时间）**：2026年08月14日 03:34:09
- **修改内容**：从原计划中独立拆出事实性验收内容，补充真实数据、路径、环境、测试和版本状态证据。
- **文档概述**：本文仅记录对 `C:\Users\24848\Desktop\tet_work` 的实际检查结果，不提出新的模型设计。验收覆盖 `amber/with_pipeline` 分支、`dataest-pipeline` 当前主分支、pipeline adapter、Hydra 配置、真实 manifest、quality report、mesh/indicator/stage-field 产物、geometry-level split、Python 环境、单元测试、真实数据构建、checkpoint 和 Git 远端状态。本文用于区分“已经实现”“在合成数据上通过”“真实数据可运行”和“效果已经证明”四个不同层次。

## 索引目录

1. [验收范围](#1-验收范围)
2. [总体结论](#2-总体结论)
3. [代码实现验收](#3-代码实现验收)
4. [数据产物验收](#4-数据产物验收)
5. [测试与环境验收](#5-测试与环境验收)
6. [真实运行验收](#6-真实运行验收)
7. [版本与产物验收](#7-版本与产物验收)
8. [问题分级](#8-问题分级)
9. [验收边界](#9-验收边界)

## 1. 验收范围

验收对象：

```text
C:\Users\24848\Desktop\tet_work\amber
C:\Users\24848\Desktop\tet_work\dataest-pipeline
C:\Users\24848\Desktop\tet_work\黄迪做的东西.md
```

验收依据：

- 仓库提交与 diff。
- Python 源代码和 YAML 配置。
- 单元测试实际执行结果。
- `default_fix_budget_run` 真实 manifest 和产物。
- 当前机器 Conda/Python 环境。
- 当前目录中的 checkpoint 和 metrics 文件。

## 2. 总体结论

**结论：有条件通过。**

已确认：黄迪同学完成了 adapter 的核心 plumbing，pipeline 的 target、indicator 和 stage field 已能映射到 AMBER 现有数据结构和物理训练路径；真实数据本身完整，split 无 geometry 泄漏。

尚不能确认：当前项目拷贝可以直接训练、正式训练结果已经产生、physics correction 提高了模型精度、stage field 优于 indicator、真实 PDE error 得到改善。

当前更准确的项目状态是：

```text
代码原型完成
真实数据生成完成
合成 adapter 测试基本覆盖
但真实目录迁移、环境复现、PDE 评估和正式结果闭环尚未完成
```

## 3. 代码实现验收

### 3.1 分支与提交

AMBER：

- 当前分支：`with_pipeline`
- `ca6d131`：接入 pipeline weighted imitation 和 physics correction。
- `0b5f0c3`：stage field 可作为 physics correction 的物理特征。
- 相对 `38db5da`：13 个文件变化，约新增 1378 行、删除 31 行。

Pipeline：

- 当前分支：`main`
- 最新本地提交：`96f3264`
- 内容包括 `default_fix_budget.yaml` 和 69 个 Mold geometry 的 pipeline 运行说明。

### 3.2 Adapter 能力

已确认 `PipelineConditionAwareDatasetPreparator` 支持：

- `sample_manifest` 和 `split_manifest`。
- train/val/test。
- `initial_mesh` 或 `coarse_mesh` 输入。
- `final_target_mesh` target。
- status、budget、PDE family、geometry、condition 过滤。
- quality verdict 过滤。
- tetra-only 与 mixed-cell tetra 筛选。
- mesh element 数量过滤。
- metadata-only `condition_spec`。

### 3.3 物理数据接入

已确认：

- `pipeline_indicator` 从 `.npy` 读取 element importance。
- element importance 可转换为 vertex importance。
- importance 复用已有投影和 linear weighting 链路。
- `stage_field` 与 `stage_field_fusion` 从 `.npz` 读取。
- stage field 使用 probe points 到 mesh vertices 的 IDW 投影。
- loss weight source 和 model physics feature source 可以独立配置。
- weighted baseline 关闭 correction branch。
- correction 可从 weighted checkpoint 初始化。

### 3.4 配置入口

已存在：

- `amber_pipeline_weighted.yaml`
- `amber_pipeline_physics_correction_codex.yaml`
- `amber_pipeline_physics_correction_stage_field.yaml`
- `pipeline_condition_aware.yaml`

尚不存在专门的 pipeline 未加权 AMBER run 配置。

## 4. 数据产物验收

### 4.1 数据规模

主数据目录：

```text
dataest-pipeline/output/condition_aware_dataset_generation/default_fix_budget_run
```

非空 manifest 记录：276。

| 项目 | 数量 |
|---|---:|
| geometry | 69 |
| train samples | 192 |
| val samples | 40 |
| test samples | 44 |
| scalar elliptic | 138 |
| linear elasticity | 138 |
| `success_budget_closed` | 174 |
| `success_near_desired_budget` | 102 |
| budget | 7000 |

### 4.2 Quality filter

`smoke_report.json` 覆盖全部 276 个样本：

- linear elasticity：34 `PASS_STRONG`，104 `PASS_WEAK`。
- scalar elliptic：2 `PASS_STRONG`，118 `PASS_WEAK`，18 `FAIL_GLOBAL_OVERREFINE`。

默认只保留 `PASS_STRONG/PASS_WEAK`，共保留 258 个样本：

| Split | Scalar | Elasticity | 合计 |
|---|---:|---:|---:|
| train | 86 | 96 | 182 |
| val | 14 | 20 | 34 |
| test | 20 | 22 | 42 |

### 4.3 Geometry split

- train：48 geometries。
- val：10 geometries。
- test：11 geometries。
- train/val overlap：0。
- train/test overlap：0。
- val/test overlap：0。

划分符合 unseen-geometry 实验的基本要求。

### 4.4 文件一致性

由于 manifest 保存旧绝对路径，本次将旧根路径只读映射到当前目录后检查 276 个样本：

- missing initial mesh：0。
- missing target mesh：0。
- missing error indicator：0。
- missing stage field：0。
- missing stage probe points：0。
- target 无 tetra：0。
- indicator 与 target tetra 数量不匹配：0。
- `s_pde_raw` 与 probe points 不匹配：0。
- 文件读取异常：0。

说明数据本身完整，主要问题是路径可移植性，而不是产物缺失。

### 4.5 数据来源边界

- `source` 全部为 `mold_local_3d`。
- 当前机器未找到 ABC STEP 或 chunk 数据。
- 因此不能将当前数据描述为 ABC 数据实验。

## 5. 测试与环境验收

### 5.1 环境

项目根 `AGENTS.md` 要求 `AMBER_neurips`，但当前 Conda 列表中不存在该环境。

仓库环境文件声明：

- Python 3.11。
- PyTorch/PyG、Lightning、scikit-fem、meshio、gmsh 等依赖。

当前可用 `demo` 环境是 Python 3.9，核心依赖基本存在，但 `torch-scatter` 动态库存在加载警告。

### 5.2 执行结果

- adapter 相关 `py_compile`：通过。
- Hydra pipeline weighted 配置组合：通过。
- adapter tests：5 项中 3 项通过、2 项失败。
- weighted/correction tests：26 项中 24 项通过、2 项失败。

四个失败由同一原因触发：Python 3.9 导入 `np.ndarray | None` 类型注解时报错。目标环境声明 Python 3.11，因此需要在 Python 3.11 中重新验收，不能据此判定算法逻辑失败，也不能忽略环境复现问题。

Pipeline pytest 本次未执行，因为当前 `demo` 环境未安装 `pytest`。

## 6. 真实运行验收

### 6.1 路径问题

真实 manifest 路径示例：

```text
D:\SITP\tet_work\dataest-pipeline\output\...
```

当前目录：

```text
C:\Users\24848\Desktop\tet_work\dataest-pipeline\output\...
```

adapter 的 `_resolve_path` 对绝对路径原样返回，没有旧根重定位。

### 6.2 实际失败表现

使用当前真实 `pipeline_output_root` 构建 datasets，最终错误为：

```text
ValueError: Pipeline split 'train' is empty after filtering.
```

根因是旧绝对路径不存在；`over_limit_policy: skip` 将 mesh 读取异常转换为样本跳过，全部样本被过滤后才报 empty split。

结论：当前拷贝尚不能零修改运行真实训练。

## 7. 版本与产物验收

### 7.1 Git 状态

- `amber/with_pipeline` 的两个 adapter 提交未出现在远端 `origin/main`。
- `dataest-pipeline/main` 比远端多一个提交。
- 两个工作树本次检查时均无未提交文件变化。

### 7.2 实验产物

当前 `amber` 目录未发现：

- `last*.ckpt`。
- 正式 `metrics.csv`。
- `evaluation_metrics.yaml`。
- W&B summary 导出。

因此无法独立核验交接说明中提到的训练 smoke checkpoint。

## 8. 问题分级

### P0：阻塞真实实验

- manifest/run YAML 绝对路径失效。
- 错误被 `skip` 掩盖，诊断不足。
- 声明环境未建立和复验。
- 缺少 pipeline 未加权 baseline。
- 缺少真实 PDE evaluator。
- 缺少正式结果包和版本绑定。

### P1：影响实验解释

- baseline 无 condition 输入，而完整方法通过 stage field 获得 condition 信息。
- 需要 feature-only 控制组隔离输入信息与 gated correction 的贡献。
- 当前只有一个 budget。
- 当前只有 Mold-derived geometry。

### P2：可后续增强

- ABC 扩展。
- 多预算泛化。
- 更多 physics field 融合。
- 额外模型 baseline。

## 9. 验收边界

本次验收可以证明：

- adapter 代码和配置确实存在。
- 数据产物完整。
- 主要接口在合成测试中得到覆盖。
- geometry split 无泄漏。

本次验收不能证明：

- weighted imitation 提高 test 性能。
- correction 优于 expert prior。
- stage field 优于 indicator。
- 真实 PDE error 降低。
- ABC 泛化成立。

