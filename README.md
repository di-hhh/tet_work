# AMBER

See `docs/condition_aware_dataset_pipeline.md` for the new condition-aware dataset generation subsystem, the unified CLI, and the recommended 3D STEP smoke validation on `data/mold` while ABC is still downloading.
