# amber 侧 pipeline 数据适配工作总结

本文只总结 `D:\SITP\tet_work\amber` 侧到目前为止已经实现的“数据适配”工作，以及这些工作相对 `D:\SITP\tet_work\dataest-pipeline\AMBER_PHYSICS_TRAINING_WITH_PIPELINE_DATA.md` 中 4.1/4.2/4.3 的满足情况和边界。

## 1. 当前分支最近 2 次 git commit

当前 `D:\SITP\tet_work\amber` 分支为 `with_pipeline`。

最近 2 次 git commit 提交如下：

1. `0b5f0c3 (HEAD -> with_pipeline) physics correction阶段可选择地使用stage_field作为模型特征通道的物理特征`
   - 修改/新增的主要文件：
     - `config/_runs/amber/amber_pipeline_physics_correction_codex.yaml`
     - `config/_runs/amber/amber_pipeline_physics_correction_stage_field.yaml`
     - `config/_runs/amber/amber_pipeline_weighted.yaml`
     - `config/task/pipeline_condition_aware.yaml`
     - `src/algorithm/core/amber.py`
     - `src/algorithm/dataloader/amber_data.py`
     - `src/algorithm/util/fem_imitation_weights.py`
     - `src/tasks/pipeline_condition_aware_dataset_preparator.py`
     - `tests/test_pipeline_condition_aware_adapter.py`
   - 主要含义：在不修改模型架构和 loss 的前提下，让 physics correction 阶段可以把 pipeline 的 `stage_field` / `stage_field_fusion` 映射为现有 1 维 `physics_feature` 输入通道。

2. `ca6d131 On branch with_pipeline.适配AMBER模型的weighted imitation和physics correction可以使用pipeline的数据`
   - 修改/新增的主要文件：
     - `config/_runs/amber/amber_pipeline_physics_correction_codex.yaml`
     - `config/_runs/amber/amber_pipeline_weighted.yaml`
     - `config/task/pipeline_condition_aware.yaml`
     - `main.py`
     - `src/algorithm/core/mesh_generation_algorithm.py`
     - `src/algorithm/dataloader/__init__.py`
     - `src/algorithm/dataloader/amber_data.py`
     - `src/algorithm/util/fem_imitation_weights.py`
     - `src/algorithm/util/interpolate_sizing_field.py`
     - `src/tasks/pipeline_condition_aware_dataset_preparator.py`
     - `tests/test_pipeline_condition_aware_adapter.py`
   - 主要含义：新增 pipeline condition-aware dataset preparator，把 pipeline 输出转成 AMBER 的 `SourceData` / `AmberData`，并让 weighted imitation 和 physics correction 能使用 pipeline 的 `error_indicator`。

## 2. 总体边界

amber 侧实现的是数据适配层 plumbing，不是新的 AMBER 模型设计。

已经保持的边界：

- 不修改当前 AMBER 模型架构。
- 不修改当前 AMBER loss 公式。
- 不让模型直接读取完整 `condition_spec`。
- `condition_spec` 只作为 metadata 存在于 `SourceData.imitation_weight_cache`。
- pipeline 数据路径、输入 mesh 模式、target 模式、status 过滤、quality 过滤、stage field 模式等都放在 YAML 配置中，不在代码中写死。
- weighted baseline 阶段默认只用 pipeline `error_indicator` 做 weighted imitation 的 loss 权重，不给模型输入 `stage_field`。
- physics correction 阶段默认仍用 pipeline `error_indicator` 做 loss 权重，同时可以用 `stage_field_fusion` 作为模型现有 1 维 `physics_feature` 输入。
- 默认只接受 `PASS_STRONG` / `PASS_WEAK` 的 smoke verdict，过滤 `FAIL_GLOBAL_OVERREFINE` 样本。
- 默认只支持 tetra cell。混合 cell mesh 默认筛出 tetra；筛完没有 tetra 就 fail。

没有实现、也不应在“不改模型架构/不改 loss”的目标下继续实现的内容：

- 没有 stage field auxiliary supervision。
- 没有 trajectory-based curriculum。
- 没有 multi-step imitation。
- 没有让模型直接消费 raw `condition_spec`。
- 没有把多维 stage fields 直接喂给模型；当前仍映射到现有 1 维物理特征通道。
- 没有新增 stage field 专属 loss。
- 没有使用 `teacher_metadata.final_allocation_diagnostics` 或 `teacher_metadata.adaptive_error_history` 作为新的监督。

## 3. 已实现的数据适配工作

### 3.1 新增 pipeline dataset preparator

核心文件：

- `D:\SITP\tet_work\amber\src\tasks\pipeline_condition_aware_dataset_preparator.py`
- `D:\SITP\tet_work\amber\src\algorithm\dataloader\__init__.py`

实现内容：

- 新增 `PipelineConditionAwareDatasetPreparator`，通过 `dataset_preparator: pipeline_condition_aware` 注册到 AMBER 数据集构建入口。
- 从 `task.pipeline_output_root/manifests/<manifest_name>.jsonl` 读取 pipeline 样本，默认 `manifest_name: sample_manifest`。
- 支持 `split_source: sample_manifest` 或 `split_source: split_manifest`。
- 支持 `train` / `val` / `test` 三个 split，并通过 `required_splits` 和 `empty_split_policy` 控制空 split 是否失败。
- 支持 `allowed_statuses`，默认：
  - `success_budget_closed`
  - `success_near_desired_budget`
- 可以通过 YAML 手动加入 `success_partial_under_budget`。
- 支持 `budget_filter`、`require_single_budget`、`pde_family_filter`、`geometry_id_filter`、`condition_id_filter`、`one_condition_per_geometry` 等过滤条件。
- 默认 `input_mesh_mode: initial_mesh`，也支持 `coarse_mesh`。
- 固定支持 `target_mode: final_target_mesh`，但该值仍在 YAML 中配置并由代码校验。
- 把 `final_target_mesh_path` 加载为 expert target mesh。
- 把 `initial_mesh_path` 或 `geometry_artifact_paths.coarse_mesh_path` 加载为 learner 输入 mesh。
- 把 `geometry_artifact_paths.source_path` 加载为几何源，用于设置 `geom_fn`。
- 把 `optional_error_indicator_path`、`optional_stage_field_path` 和样本 metadata 写入 `SourceData.imitation_weight_cache`。
- 把 `condition_spec` 保留在 metadata 中，但不转成模型输入。

### 3.2 质量过滤和 tetra cell 过滤

核心文件：

- `D:\SITP\tet_work\amber\src\tasks\pipeline_condition_aware_dataset_preparator.py`
- `D:\SITP\tet_work\amber\config\task\pipeline_condition_aware.yaml`

实现内容：

- 默认开启 `quality_filter.enabled: true`。
- 默认读取 `reports/smoke_report.json`。
- 从 `geometry_reports`、`scalar_smoke.geometry_reports`、`elasticity_smoke.geometry_reports` 中建立 `sample_id -> verdict` 映射。
- 默认 `allowed_verdicts`：
  - `PASS_STRONG`
  - `PASS_WEAK`
- 因此默认过滤 `FAIL_GLOBAL_OVERREFINE`。
- 默认 `missing_report_policy: fail`，报告缺失时失败。
- 默认 `missing_sample_policy: fail`，样本没有 verdict 时失败。
- 默认 `mesh_cell_type: tetra`。
- 默认 `cell_type_policy: filter_tetra_then_fail_if_empty`：
  - 混合 cell mesh 中筛出 tetra。
  - 如果没有 tetra，则报错。
- 也支持 `strict_tetra_only`，用于要求 mesh 中不能有非 tetra cell。

### 3.3 pipeline indicator 接入 weighted imitation

核心文件：

- `D:\SITP\tet_work\amber\src\algorithm\util\fem_imitation_weights.py`
- `D:\SITP\tet_work\amber\src\algorithm\dataloader\amber_data.py`
- `D:\SITP\tet_work\amber\src\algorithm\loss\amber_loss.py`

实现内容：

- 在 `get_imitation_weight_bundle` 中新增 pipeline 来源：
  - `pipeline_indicator`
  - `stage_field`
  - `stage_field_fusion`
- 默认 weighted baseline 配置使用：
  - `task.physics_weight_source: pipeline_indicator`
  - `algorithm.weighted_imitation.weight_source_mode: pipeline_indicator`
- `pipeline_indicator` 从 `optional_error_indicator_path` 指向的 `.npy` 文件读取。
- 读取后校验 indicator 长度必须匹配 expert mesh element 数量。
- element-level indicator 会转成 vertex-level importance，以复用 AMBER 现有投影和权重构造链路。
- 最终仍由现有 `AmberLoss` 从 `graph_batch.imitation_weights` 读取权重，计算当前已有的 weighted imitation loss。
- 这意味着没有新增 loss，只是把现有 weighted imitation 的权重来源从旧的 Console/Mold reference 扩展到了 pipeline teacher 的 `error_indicator`。

### 3.4 stage field / stage field fusion 接入现有 1 维 physics feature

核心文件：

- `D:\SITP\tet_work\amber\src\algorithm\util\fem_imitation_weights.py`
- `D:\SITP\tet_work\amber\src\algorithm\dataloader\amber_data.py`
- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_physics_correction_stage_field.yaml`

实现内容：

- 支持从 `optional_stage_field_path` 指向的 `.npz` 文件读取 stage fields。
- 要求 `.npz` 中包含 `probe_points`。
- 默认使用 `stage_field.mode: single_field`。
- 默认字段为 `s_pde_raw`。
- 默认处理方式：
  - `direction: high_is_important`
  - `transform: log1p`
  - `normalize: robust_minmax`
- 支持 `stage_field_fusion` 的 `weighted_mean`，可把多个 stage field 先各自转成 importance 后加权融合成 1 维 importance。
- 支持 IDW 投影，把 `probe_points` 上的 stage field 投影到 expert mesh vertices。
- 投影后的 vertex importance 会进一步生成 element importance，从而复用现有 importance/weight bundle 格式。
- 在 physics correction 阶段，`AmberData` 会读取 `SourceData.imitation_weight_cache["physics_feature_source"]`，独立决定模型输入的 `physics_feature` 来源。
- 当前 stage-field physics correction 默认配置为：
  - `task.physics_weight_source: pipeline_indicator`
  - `task.physics_feature_source: stage_field_fusion`
  - `algorithm.weighted_imitation.weight_source_mode: pipeline_indicator`
- 也就是说：loss 权重语义仍由 pipeline `error_indicator` 定义；模型侧物理特征由 `stage_field_fusion` 提供。

### 3.5 loss 权重来源和模型物理特征来源解耦

核心文件：

- `D:\SITP\tet_work\amber\src\algorithm\dataloader\amber_data.py`
- `D:\SITP\tet_work\amber\src\algorithm\util\fem_imitation_weights.py`

实现内容：

- `weight_source_mode` 仍用于构造 imitation loss weights。
- `physics_feature_source` 单独用于构造 physics correction branch 看到的 `physics_feature`。
- 这样允许 physics correction 阶段使用：
  - loss 权重：`pipeline_indicator`
  - 模型物理特征：`stage_field_fusion`
- 该解耦不改变 loss，只是让 `AmberData` 在组图时从另一个来源生成已有的 `physics_feature`。
- 如果 physics correction branch 未启用，则 `_add_physics_feature` 直接返回，不向图输入追加物理特征。
- weighted baseline 配置中 `algorithm.enable_physics_correction_branch: False`，因此 weighted baseline 阶段不会让模型看到 stage field。

### 3.6 physics correction 训练入口配置

核心文件：

- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_weighted.yaml`
- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_physics_correction_codex.yaml`
- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_physics_correction_stage_field.yaml`

实现内容：

- 新增/维护 pipeline weighted baseline 配置：
  - `amber_pipeline_weighted`
  - 关闭 physics correction branch。
  - 使用 pipeline `error_indicator` 作为 weighted imitation 权重。
- 新增/维护 pipeline physics correction 配置：
  - `amber_pipeline_physics_correction_codex`
  - 开启 physics correction branch。
  - 默认 indicator 既作为 loss 权重，也作为 physics feature 来源。
- 新增 stage-field physics correction 配置：
  - `amber_pipeline_physics_correction_stage_field`
  - 开启 physics correction branch。
  - loss 权重仍用 `pipeline_indicator`。
  - 模型物理特征使用 `stage_field_fusion`，默认单字段 `s_pde_raw`。
- 三个配置默认指向：
  - `D:/SITP/tet_work/dataest-pipeline/output/condition_aware_dataset_generation/default_fix_budget_run`
- 该路径只是 YAML 默认值，不写死在代码中；可在命令行覆盖为：
  - `D:/SITP/tet_work/dataest-pipeline/output/condition_aware_dataset_generation/smoke_console_layered`
  - 或其他同结构 pipeline 输出目录。

### 3.7 从 weighted baseline checkpoint 初始化 physics correction

核心文件：

- `D:\SITP\tet_work\amber\src\algorithm\core\mesh_generation_algorithm.py`
- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_physics_correction_stage_field.yaml`
- `D:\SITP\tet_work\amber\config\_runs\amber\amber_pipeline_physics_correction_codex.yaml`

实现内容：

- 配置中保留 `algorithm.init_from_weighted_baseline_checkpoint: null`。
- 运行 physics correction 时可以通过命令行覆盖该 checkpoint 路径。
- `initialize_from_weighted_baseline_checkpoint_codex` 会加载已有 weighted baseline checkpoint 中能匹配的参数。
- 对 shape 不一致但可适配的 tensor 做适配；无法适配的跳过。
- 这支持文档中的流程：
  - Step 3：先训练 weighted baseline。
  - Step 4：再从 weighted baseline checkpoint 继续训练 physics correction。

注意：这条 checkpoint 初始化路径属于训练流程衔接能力，不是 pipeline 数据本身的读取逻辑；但它使 pipeline weighted baseline 到 pipeline physics correction 的渐进式训练路径可用。

### 3.8 诊断和测试

核心文件：

- `D:\SITP\tet_work\amber\src\algorithm\core\amber.py`
- `D:\SITP\tet_work\amber\tests\test_pipeline_condition_aware_adapter.py`

实现内容：

- 训练日志中增加数据来源诊断：
  - `physics_feature_mean`
  - `physics_feature_std`
  - `physics_feature_stage_field_loaded`
  - `physics_feature_pipeline_indicator_loaded`
- 这些只是诊断标量，不改变模型前向和 loss。
- 单元测试覆盖：
  - pipeline adapter 能加载 tetra records。
  - pipeline `error_indicator` 能接到 imitation weights 和 physics feature。
  - `stage_field_fusion` 能作为 physics correction 的模型物理特征，同时 loss 权重仍保持 `pipeline_indicator`。
  - 默认 quality filter 会排除 `FAIL_GLOBAL_OVERREFINE`。
  - `allowed_statuses` 和 `split_source` 可通过 YAML/override 配置。
  - 默认混合 cell 策略会筛 tetra，并在没有 tetra 时失败。

## 4. 对照需求文档 4.1/4.2/4.3

### 4.1 第一层：把 pipeline 数据作为新的 expert dataset

需求文档 4.1 的核心要求：

- 用 pipeline 生成 dataset。
- 把 `final_target_mesh` 视为新的 expert target。
- 用 `initial_mesh` 或 `coarse mesh` 作为 learner 的输入起点。
- 让 AMBER 先学会模仿更强 teacher。

当前满足情况：已满足。

代码对应：

- `PipelineConditionAwareDatasetPreparator` 读取 pipeline manifest。
- `target_mode: final_target_mesh`，代码中校验只支持该 target。
- `input_mesh_mode: initial_mesh` 默认，也支持 `coarse_mesh`。
- `SourceData.expert_mesh` 来自 `final_target_mesh_path`。
- `SourceData.initial_mesh` 来自 `initial_mesh_path` 或 `coarse_mesh_path`。
- `condition_spec`、`pde_family`、`budget` 进入 metadata cache。
- `train` / `val` / `test` split 完整接入 AMBER dataset 构建。

边界：

- 当前只接 tetra mesh。
- 默认质量过滤只保留 `PASS_STRONG` / `PASS_WEAK`。
- 默认要求单一 budget，除非 YAML 关闭或指定过滤。

### 4.2 第二层：把 pipeline 的物理/重要性中间场接到 weighted imitation

需求文档 4.2 的核心要求：

- weighted imitation 可以利用 pipeline teacher 生成过程中的样本级物理重要性。
- 推荐来源包括 `error_indicator`、`stage_fields['s_pde_raw']`、`stage_fields['h_pde_only']` 等。

当前满足情况：核心路径已满足。

当前默认实现：

- weighted baseline 阶段使用 pipeline `optional_error_indicator_path` 对应的 `error_indicator.npy`。
- `error_indicator` 转成 AMBER 现有 `imitation_weights`。
- 现有 `AmberLoss` 使用 `graph_batch.imitation_weights` 计算 weighted imitation loss。
- 这使 weighted imitation 不再依赖旧的 Console/Mold reference，而可以直接使用 pipeline teacher 的样本级重要性。

当前可选实现：

- `stage_field` / `stage_field_fusion` 也可以通过 YAML 配成权重来源。
- 但按当前会话确认的默认策略，weighted baseline 不使用 stage field；stage field 优先用于 physics correction 的模型物理特征。

边界：

- 没有把 `teacher_metadata.final_allocation_diagnostics` 或 `teacher_metadata.adaptive_error_history` 接入 weighted imitation。
- 没有显式导出或使用 `importance` 字段。
- 没有使用 final allocation summary / hotspot mask。
- 这些不是当前“不改模型架构/不改 loss”的必要项。

### 4.3 第三层：让 physics correction branch 学 sample-specific residual

需求文档 4.3 的核心要求：

- 当前 physics correction 本质是：
  - `delta_expert` 先给出 expert prior。
  - `delta_phys` 再给出 physics residual。
  - `alpha` 控制 correction 强度。
- 接入 pipeline 后，希望 residual 变成针对 condition-aware teacher target 的条件残差修正。

当前满足情况：在“不改模型架构/不改 loss”的边界内，已最大限度满足。

当前实现方式：

- physics correction branch 仍使用 AMBER 现有结构。
- `mesh_generation_algorithm.py` 中仍是：
  - `semantic_expert_delta = denormalize(expert_output)`
  - `semantic_phys_delta = denormalize(physics_output)`
  - `applied_correction = gate * semantic_phys_delta`
  - `semantic_total_delta = semantic_expert_delta + applied_correction`
- loss 仍针对 final prediction 和 `final_target_mesh` 标签计算。
- correction auxiliary loss 仍使用已有逻辑：`target_correction = labels_for_loss - semantic_expert_delta.detach()`。
- pipeline 接入后，labels 来自 condition-aware pipeline 的 `final_target_mesh`，所以 residual 学习目标自然变成相对 pipeline teacher target 的 residual。
- `amber_pipeline_physics_correction_stage_field` 让 physics correction branch 看到由 pipeline `stage_field` 映射来的 1 维 `physics_feature`。

边界：

- 没有新建 condition-aware residual loss。
- 没有让模型直接读取完整 condition。
- 没有新增输出头。
- 没有 stage field auxiliary supervision。
- 没有 trajectory curriculum 或多步 imitation。
- 因此它是 4.3 的 adapter-only 最小实现，不是 4.3 的所有潜在扩展。

## 5. 当前是否保持 AMBER 的两个新增能力

### 5.1 weighted imitation

已保持。

保持方式：

- `AmberLoss` 仍使用现有 weighted imitation 逻辑。
- `imitation_weights` 仍由 `AmberData` 放入 graph batch。
- pipeline adapter 只是把权重来源换成 pipeline `error_indicator`。
- 默认 weighted baseline 不打开 physics correction branch，不给模型输入 stage field。

相对旧的“假物理信息”：

- 现在 weighted imitation 的权重来自 pipeline teacher 生产过程中的真实样本级 `error_indicator`。
- 因此物理语义更贴近 pipeline teacher。
- 但这不等于已经证明最终训练精度更高；精度需要完整训练和评估确认。

### 5.2 physics correction branch

已保持。

保持方式：

- 模型侧 physics correction branch 的结构没有重写。
- 仍是 expert prior + physics residual + gate。
- `physics_feature` 仍是当前模型已支持的直接物理特征通道。
- pipeline adapter 只是把 `physics_feature` 的来源从旧参考信息扩展为 pipeline `stage_field_fusion`。
- checkpoint 初始化路径支持“先训 weighted baseline，再初始化 physics correction”。

相对旧的“假物理信息”：

- 当前 stage-field 配置能让 physics correction branch 看到 pipeline teacher 中间物理场映射后的 1 维特征。
- 因此比旧的假/手工物理信息更接近 teacher 生成过程。
- 但是否“更强”仍需要完整训练曲线、val/test 指标和 ablation 证明。

## 6. 会话中确认并真正落地的配置决策

已经落地到 YAML 或代码校验的决策：

- `input_mesh_mode` 默认 `initial_mesh`，不写死，可 YAML 配置。
- `target_mode` 为 `final_target_mesh`，不写死，可 YAML 配置，但当前代码只支持该模式。
- `allowed_statuses` 默认 `[success_budget_closed, success_near_desired_budget]`，可手动加入 `success_partial_under_budget`。
- `split_source` 可配置为 `sample_manifest` 或 `split_manifest`。
- 默认只保留 `PASS_STRONG` / `PASS_WEAK`，过滤 `FAIL_GLOBAL_OVERREFINE`。
- `condition_spec_mode: metadata_only`，不作为模型输入。
- weighted baseline 阶段只用 `pipeline_indicator` 作为 loss 权重。
- weighted baseline 阶段不使用 stage field 作为模型特征。
- physics correction 阶段默认 loss 权重仍用 `pipeline_indicator`。
- physics correction 阶段默认模型物理特征可用 `stage_field_fusion`。
- 默认 stage field 为 `s_pde_raw`。
- stage field 默认先 `log1p`，再 robust min-max 归一化。
- stage field 从 probe points 到 mesh vertices 使用 IDW 投影。
- stage field 可以配置为单字段或 weighted mean 融合，但最终仍降到 1 维。
- 只支持 `pipeline_indicator`、`stage_field`、`stage_field_fusion` 三类 pipeline 物理来源。
- 混合 cell 默认筛 tetra；筛完为空则 fail。
- pipeline dataset root 只在 YAML 默认值中设置，不写死在 Python 代码中。

没有落地的内容：

- 不直接使用 raw `condition_spec`。
- 不需要 `stage_fields` 和 `condition_spec` 同时进入模型。
- 不做 stage field auxiliary supervision。
- 不做 trajectory-based curriculum。
- 不做多步 imitation。
- 不把 stage field 作为 weighted baseline 的默认模型输入。

## 7. 已验证内容

会话中已经进行过的验证包括：

- `python -m py_compile` 检查过相关 Python 文件语法。
- `python -m unittest tests.test_pipeline_condition_aware_adapter` 通过。
- `python -m unittest tests.test_weighted_imitation tests.test_weighted_imitation_diagnostics_codex test_physics_correction_codex` 通过。
- 使用真实 pipeline 输出目录做过 dataloader 检查，确认：
  - `weight_source` 为 `pipeline_indicator`。
  - `feature_source` 为 `stage_field_fusion`。
  - `quality_verdict` 为 `PASS_WEAK`。
  - `physics_feature_stage_field_loaded` 为 `1.0`。
  - `physics_feature_pipeline_indicator_loaded` 为 `0.0`。
  - `physics_feature_available` 为 `1.0`。
- 用户手动跑过 `amber_pipeline_physics_correction_stage_field` 的最小训练 smoke，并产生 `last-v2.ckpt`。

这些验证能证明：

- amber 能读取 pipeline 数据。
- pipeline 数据能进入 train/val/test 数据构建流程。
- weighted imitation 能使用 pipeline `error_indicator`。
- physics correction 阶段能使用 pipeline `stage_field` 映射出的 `physics_feature`。
- 从 weighted baseline checkpoint 初始化 physics correction 的路径可用。

这些验证不能单独证明：

- 最终模型精度提升。
- stage field 一定优于只用 error indicator。
- 完整训练收敛质量。
- 所有 pipeline 数据集变体都无异常。

## 8. 最终结论

当前 amber 侧已经实现的是：把 `dataest-pipeline` 生产的 condition-aware dataset 接入 AMBER 的现有数据、weighted imitation、physics correction 训练流程。

对需求文档的满足情况：

- 4.1 `把 pipeline 数据作为新的 expert dataset`：已满足。
- 4.2 `把 pipeline 的物理/重要性中间场接到 weighted imitation`：核心路径已满足，默认用 `error_indicator`；stage field 也可作为配置来源，但默认优先用于 physics correction 特征。
- 4.3 `让 physics correction branch 学 sample-specific residual`：在不修改模型架构和 loss 的边界内已最大限度满足；通过 pipeline final target 作为标签、pipeline indicator 作为 loss 权重、pipeline stage field 作为现有 1 维 physics feature，使现有 expert prior + physics residual + gate 机制作用在 pipeline teacher target 上。

当前应该停止在这里的内容：

- 如果继续做 stage field auxiliary supervision，需要新增辅助监督目标和 loss，超出“不改 loss”边界。
- 如果继续做 trajectory-based curriculum 或多步 imitation，需要改变训练调度/数据组织，通常也会改变监督层次，超出当前 adapter-only 边界。
- 如果让模型直接读完整 condition_spec 或多维 stage_fields，通常需要调整输入维度、模型前向或配置约束，超出当前“不改模型架构”的边界。
