# @package _global_
# [Claude Code] 新增：使用 U-Net 架构的 mold 任务运行配置。

defaults:
  - _amber_unet
  - override /task: mold
  - _self_

exp_name: amber_unet_mold
_version: 1

trainer:
  max_epochs: 201
idx: unet100

hydra:
  sweeper:
    ablative_params:
      - task.max_initial_element_volume: 0.000001
        algorithm.inference_steps: 1
        idx: unet_stp100
